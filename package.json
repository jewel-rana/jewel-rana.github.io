{
  "name": "jewel-rana-cv",
  "version": "1.0.0",
  "private": true,
  "description": "Professional CV and GitHub Pages portfolio for Md Jewel Rana",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-cv.js",
    "build": "node scripts/build-site.js",
    "build:all": "npm run generate && npm run build",
    "test": "node --test tests/*.test.js",
    "preview": "npx --yes serve dist -l 4173",
    "verify": "npm run build:all && npm test"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "docx": "^9.5.0",
    "puppeteer": "^24.12.1"
  }
}
